spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipe_dev_2
    username: recipe_dev_user
    password: password
    platform: mysql
    initialization-mode: always

  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: dbinitscript.sql